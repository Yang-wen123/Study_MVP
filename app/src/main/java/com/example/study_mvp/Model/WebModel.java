package com.example.study_mvp.Model;

import com.example.study_mvp.Bean.MyPicture;
import com.example.study_mvp.Presenter.onWebListener;

public interface WebModel {
    void geturl(onWebListener onWebListener);
}
